# ============================================================================
# CE365 Agent - Configuration
# ============================================================================
# Community Edition v2.0.0
# Kopiere diese Datei zu .env und fülle deine API-Keys ein
#
# Beim ersten Start führt dich ein Setup-Wizard durch die Konfiguration!
# ============================================================================

# LLM Provider: "anthropic", "openai", "openrouter"
LLM_PROVIDER=anthropic

# API Keys (nur den Key des gewählten Providers setzen)
ANTHROPIC_API_KEY=your_api_key_here
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Optional: Log Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: LLM Model (Provider-abhängig)
LLM_MODEL=claude-sonnet-4-5-20250929

# ============================================================================
# LEARNING SYSTEM - DATABASE KONFIGURATION
# ============================================================================

# Database Type: "sqlite" (lokal) oder "postgresql" (remote, Pro)
LEARNING_DB_TYPE=sqlite

# PostgreSQL URL (nur wenn LEARNING_DB_TYPE=postgresql)
# Format: postgresql://username:password@hostname:port/database
LEARNING_DB_URL=

# Lokaler Fallback (wird verwendet wenn Remote-DB nicht erreichbar)
LEARNING_DB_FALLBACK=data/cases.db

# Connection Timeout in Sekunden
LEARNING_DB_TIMEOUT=5

# Anzahl Retry-Versuche bei Connection-Fehler
LEARNING_DB_RETRY=3

# ============================================================================
# SECURITY - PII DETECTION (Presidio)
# ============================================================================

# PII Detection aktivieren (empfohlen!)
PII_DETECTION_ENABLED=true

# Detection Level: "high" (alle PII), "medium" (wichtige), "low" (kritische)
PII_DETECTION_LEVEL=high

# User-Warnings anzeigen wenn PII erkannt wurde
PII_SHOW_WARNINGS=true

# ============================================================================
# LICENSE (Edition & Key)
# ============================================================================

# Edition: "community", "pro"
EDITION=community

# Lizenzschlüssel (wird beim Setup konfiguriert, nur Pro)
LICENSE_KEY=

# Lizenzserver URL (nur Pro)
LICENSE_SERVER_URL=

# ============================================================================
# SECURITY - TECHNICIAN PASSWORD
# ============================================================================

# Techniker-Passwort (bcrypt hash)
# Wird beim Setup erstellt oder mit: ce365 --set-password
TECHNICIAN_PASSWORD_HASH=

# Session-Timeout in Sekunden (Standard: 1 Stunde)
SESSION_TIMEOUT=3600
