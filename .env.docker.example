# ============================================================================
# CE365 Agent - Docker Deployment Konfiguration (EXAMPLE)
# ============================================================================
# Kopiere diese Datei zu .env und fülle die Werte aus
# ODER nutze das install.sh Script für interaktive Konfiguration
# ============================================================================

# EDITION & LICENSE
# free, pro, business
EDITION=free
LICENSE_KEY=
LICENSE_SERVER_URL=https://license.ce365.local

# DOCKER REGISTRY
DOCKER_REGISTRY=registry.ce365.local
DOCKER_REGISTRY_USER=
DOCKER_REGISTRY_PASSWORD=
VERSION=latest

# NETWORK
# Deine Zugriffs-URL (wird vom Installer gesetzt)
API_URL=http://localhost
HTTP_PORT=80
HTTPS_PORT=443

# CLOUDFLARE TUNNEL (Optional - nur wenn Profil "cloudflare" aktiv)
CLOUDFLARE_TUNNEL_TOKEN=

# TAILSCALE (Optional - nur wenn Profil "tailscale" aktiv)
TAILSCALE_AUTH_KEY=
TAILSCALE_HOSTNAME=ce365

# ANTHROPIC API
# Erstelle einen Key: https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-your-key-here
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# DATABASE
# PostgreSQL für Pro Business / Enterprise
POSTGRES_DB=ce365
POSTGRES_USER=ce365
POSTGRES_PASSWORD=generate-secure-password-here

# REDIS
# Wird automatisch generiert vom Installer
REDIS_PASSWORD=generate-secure-password-here

# SECURITY
# Wird automatisch generiert vom Installer (openssl rand -base64 32)
SECRET_KEY=generate-secret-key-here
JWT_SECRET=generate-jwt-secret-here

# OPTIONAL FEATURES
PII_DETECTION_ENABLED=true
WEB_SEARCH_ENABLED=true

# LOGGING
# DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# WATCHTOWER (Auto-Update)
# Optional: Email-Benachrichtigungen bei Updates
# Format: "email" WATCHTOWER_NOTIFICATION_EMAIL_TO
WATCHTOWER_NOTIFICATIONS=

# DOCKER COMPOSE PROFILES
# Komma-separierte Liste: cloudflare, tailscale, autoupdate
# Beispiele:
#   COMPOSE_PROFILES=cloudflare,autoupdate
#   COMPOSE_PROFILES=tailscale
#   COMPOSE_PROFILES=
COMPOSE_PROFILES=
